- MNIST

-- To use the Weston-Watkins SVM, run 

./train -s 30 -c 1 data/mnist.scale

./predict data/mnist.scale.t mnist.scale.model output

-- To train Crammer Singer

./train -s 4 data/mnist.scale

./predict data/mnist.scale.t mnist.scale.model output


- USPS

-- To use the Weston-Watkins SVM, run 

./train -s 30 -c .1 data/usps

./predict data/usps.t usps.model output

-- To train Crammer Singer

./train -s 4 data/usps

./predict data/usps.t usps.model output


- CIFAR10

WW 
./train -s 30 -c .1 data/cifar10

./predict data/cifar10.t cifar10.model output

CS

./train -s 4 data/cifar10

./predict data/cifar10.t cifar10.model output



./train -s 30 -c 1 data/toy

./predict data/toy toy.model output


./train -s 30 -c 1 data/dna.scale

./predict data/dna.scale.t dna.scale.model output

./train -s 4 -c 1 data/dna.scale

./predict data/dna.scale.t dna.scale.model output



ORIGINAL LIBLINEAR README
